# KLEP decisions

This log separates direct owner rules from rewrite choices that still need the
owner's visual/semantic approval. `Accepted` means the owner stated the rule in
this task or supplied it as the decision in the Key/Lock handoff. `Candidate`
means the rewrite implements the rule for inspection, but the owner has not yet
accepted it as permanent KLEP behavior. `Superseded` retains an earlier rule as
history after a later Accepted decision replaced it; it is no longer active.

| ID | Status | Rule | Source |
|---|---|---|---|
| KLEP-KEY-001 | Accepted | A Unity Key asset is reusable authored definition data, never mutable runtime state. Runtime facts belong to stores and snapshots. | Owner-supplied Key/Lock handoff |
| KLEP-KEY-002 | Accepted | Keys are Local unless their definition is explicitly marked Global. Global state is not ambient/static. | Owner statement, 2026-07-18 |
| KLEP-KEY-003 | Accepted | Lock math is explicit `All`, `Any`, and `Not`; a negative Health condition succeeds only when Health is absent. | Owner statement, 2026-07-18 |
| KLEP-KEY-004 | Accepted | Health carries HP. A consume-at-zero behavior removes Health, and the following absence enables the death-animation behavior. | Owner statement, 2026-07-18 |
| KLEP-KEY-005 | Superseded | `KLEPNeuron.Tick` is the single source of Core timing; Unity `Update` and `FixedUpdate` are not separate decision domains. Every evaluation snapshot is immutable, while one Tick may publish a new Local snapshot at a deterministic tandem-wave barrier. | Superseded by accepted KLEP-KEY-016 and KLEP-AGENT-008, 2026-07-19 |
| KLEP-KEY-006 | Accepted | Stable Key identity is the authored asset GUID / Core `KLEPKeyId`; display names and payloads are not identity. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-007 | Accepted | Several occurrences of one Key ID may coexist. Mutation authority is one exact `KLEPKeyFact`, not a name or textual occurrence ID. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-008 | Accepted | The first Core payload supports only immutable Boolean, Int64, finite Double, and Text fields in ordinal field order. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-KEY-009 | Accepted | Lifetimes in this slice are `OneCycle` and `Persistent`. There is no automatic consume, use-count, reservation, or return policy. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-010 | Accepted | One explicit world owner commits a Global store. Neurons read it and may catch up to its current boundary; they never advance it. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-KEY-011 | Accepted | V1 cross-neuron operations accept only an exact activated Persistent Local fact. Global and OneCycle facts are rejected. Delivery creates a new recipient occurrence with new store authority and recipient-local issued and activated ticks while preserving definition, payload, lifetime, and original `SourceId`. Same-ID occurrences coexist; delivery never implicitly replaces one. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-012 | Accepted | `CopyKey` retains the exact source fact and stages a recipient delivery. `GiveKey` is an owner-authorized move that stages exact source removal and recipient delivery. `TakeKey` has the same move effect but requires the exact donor-issued grant for that recipient and fact. `TradeKeys` is two owner-authorized Gives whose removals and deliveries are staged all-or-none. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-013 | Accepted | `RequestKey` is an immutable addressed intent only. It grants no fact authority and does not copy, remove, replace, move, or deliver a fact; any response is a separate explicit operation. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-014 | Accepted | Cross-neuron operations never alter current snapshots. A staged source removal and recipient delivery become visible at each store's next top-level Local Tick boundary, never a within-Tick Tandem wave. Trade atomicity is staging atomicity, not globally simultaneous visibility. Lock evaluation remains pure and cannot stage an interaction. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-015 | Accepted | Key interaction transport preserves payloads opaquely and neither requires nor hardcodes domain fields. In particular, V1 requires no ammo field or `observedWorldTick` field. | Owner-approved generic V1 Key interaction semantics, 2026-07-18 |
| KLEP-KEY-016 | Accepted | A Neuron is a passive, guarded substrate that owns its Local Key store, observes its injected Global store, and stores an immutable-rooted Executable catalog plus staged catalog changes. It owns no selection, firing, runtime, Goal-progress, or decision-trace authority. Exactly one Agent may hold its decision-boundary lease. The Neuron exposes immutable Key/catalog snapshots, atomic storage operations, a monotonically changing catalog revision, and stable registration-tenure identity; only its owning Agent may advance the Local decision boundary. | Owner architecture correction, 2026-07-19 |
| KLEP-EXE-001 | Accepted | Keys are tested through Locks to determine whether an Executable is available for consideration. | Owner statement, 2026-07-18 |
| KLEP-EXE-002 | Accepted | `KLEPGoal` remains a dedicated script and inherits from `KLEPExecutableBase`. | Owner statement, 2026-07-18 |
| KLEP-EXE-003 | Accepted | Executables preserve separate Validation and Execution Lock groups. Every Lock in both groups must pass against the same immutable cycle snapshot before the Executable may be scored. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EXE-004 | Accepted | Executable Lock evaluation returns both group traces, evaluates every authored Lock for inspectability, preserves authored order, and mutates neither Keys nor Locks. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EXE-005 | Superseded | The first sealed score is `BaseAttractiveness` plus the authored attractiveness of every Validation and Execution Lock. Its component trace is immutable and finite. Key attractiveness is excluded because `Any` and `Not` make the legacy key formula incorrect and ambiguous. | Superseded by accepted KLEP-EXE-024, 2026-07-19 |
| KLEP-EXE-006 | Superseded | A Goal is structurally an Executable in this slice, but Goal layers, progress, proxy scoring, activation outputs, and child ownership have no runtime behavior until their contracts are approved. | Superseded by accepted KLEP-EXE-012, 2026-07-18 |
| KLEP-EXE-007 | Accepted | Executable IDs compare ordinally. Re-registering the same object is idempotent; a different object with the same ID is rejected unless the old ID was explicitly staged for removal first. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EXE-008 | Superseded | An Executable initializes once when its registration commits. A run enters once, ticks without re-entering, and terminates as Succeeded, Failed, or Cancelled. Exit and Cleanup occur exactly once; a terminal run cannot re-enter until the next Neuron Tick. | Superseded by accepted KLEP-EXE-026, 2026-07-19 |
| KLEP-EXE-009 | Superseded | Every eligible Tandem Executable advances at most once per Neuron Tick. Actual Local Key changes publish together at a wave barrier and may unlock previously blocked Tandems. After settlement, at most one Solo Executable is selected from the final snapshot. | Superseded by accepted KLEP-EXE-026, 2026-07-19 |
| KLEP-EXE-010 | Superseded | Patient means no Solo behavior is selected or running; the Neuron is waiting for stimuli. A strictly higher-scoring Solo may interrupt the current Solo, while an equal score retains the current Solo. Confidence-derived switching margin is deferred to the Agent phase. | Superseded by accepted KLEP-EXE-026, 2026-07-19 |
| KLEP-EXE-011 | Superseded | Executables declare the stable Key IDs they may emit. Only actual emissions cause a tandem recheck. Local Tandem outputs become visible at the next same-Tick wave barrier; Solo outputs become visible at the next Neuron Tick. Global outputs remain staged for the world-owned Global boundary. | Superseded by accepted KLEP-EXE-025 and KLEP-EXE-027, 2026-07-19 |
| KLEP-EXE-012 | Superseded | Goal children are exclusively Goal-owned. A Goal advances at most one layer per Tick: All requires every child to succeed, Any requires at least one actual child success, and None skips without executing children. Goals use their own score, emit activation once on Enter, and keep completion observable for the rest of the Tick. | Superseded by accepted KLEP-EXE-027 and KLEP-EXE-028, 2026-07-19 |
| KLEP-EXE-013 | Accepted | An execution exception is recorded, the entered Executable exits and cleans up exactly once, and the exception is rethrown rather than silently hidden. | Owner approval, 2026-07-18 |
| KLEP-EXE-014 | Superseded | Core has no Executable Update/FixedUpdate split. `KLEPNeuron.Tick` alone advances initialization, tandem waves, Solo arbitration, Goals, and lifecycle. | Superseded by accepted KLEP-AGENT-008 and KLEP-EXE-026, 2026-07-19 |
| KLEP-EXE-015 | Accepted | One output batch is preflighted across Local and Global stores and staged atomically. Any failure restores the complete prior pending state; an Add whose stable Key ID is visible or pending in the opposite scope is rejected before mutation. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EXE-016 | Accepted | If a Tandem wave aborts on an exception, it publishes no peer output. Any earlier peer left Running is cancelled with `WaveAborted`, exits and cleans once, then may rearm on the next top-level Tick so its discarded output can be reproduced. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EXE-017 | Accepted | Initialization output is part of the registration boundary. If its output batch is invalid, pending Key changes roll back and every registration in that boundary is rejected rather than continuing without its one-time output. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EXE-018 | Superseded | A fault inside a Goal-owned child unwinds the child and Goal but preserves the actual child stable ID and lifecycle stage in the Neuron fault trace while rethrowing the original exception. | Superseded only in trace ownership by accepted KLEP-EXE-026; fault and unwind behavior remains protected, 2026-07-19 |
| KLEP-EXE-019 | Superseded | Goal teardown attempts cancellation for every Running child even when an earlier child's Exit or Cleanup faults. A single fault is rethrown unchanged; multiple child faults are aggregated after all cancellation attempts, with the first failing child retained in the Neuron trace. | Superseded only in trace ownership by accepted KLEP-EXE-026; teardown behavior remains protected, 2026-07-19 |
| KLEP-EXE-020 | Accepted | Goal construction preflights duplicate identity and all existing Goal/Neuron ownership for the complete child set before claiming any child. Rejected construction leaves every child's ownership unchanged. | Owner-reported ownership bug and requested correction, 2026-07-19 |
| KLEP-EXE-021 | Accepted | When an Executable Tick returns `Failed`, every Key output queued during that run's Enter/Tick context is discarded before output application. The trace records a terminal Failed result with the matching Failed exit reason and no outputs; Exit and Cleanup still occur exactly once, no Key mutation is staged, and Solo arbitration does not fall back to another behavior in that Tick. | Owner decision preserving current runtime plus focused regression, 2026-07-19 |
| KLEP-EXE-022 | Accepted | A child `Failed` result does not itself fail a Goal in V1. In `AllMustFire`, each eligible unsatisfied child advances in authored order, successful-child progress is retained, failed children remain unsatisfied and may retry on a later top-level Tick, and the layer completes only after every child actually succeeds. In `AnyCanFire`, children are considered serially in authored order: a blocked or Failed child permits the next sibling to be considered in the same Tick, the first Running child retains the layer and stops later siblings for that Tick, and the first Succeeded child completes the layer without advancing later siblings. If no child succeeds the Goal remains Running. Running children are cancelled only when their own Locks close or the Goal exits; there is no proactive sibling cancellation. | Owner decision preserving current runtime plus focused regression, 2026-07-19 |
| KLEP-EXE-023 | Superseded | A Tandem that has already advanced in a top-level Neuron Tick is not reevaluated, advanced, or cancelled during later waves of that same Tick, even if later-wave Key publication closes its Locks. If it remains Running, the following top-level Tick reevaluates it before advancement; closed Locks then cancel it with `LocksClosed`, followed by exact-once Exit and Cleanup. | Superseded only in Tick ownership by accepted KLEP-EXE-026; settlement behavior remains protected, 2026-07-19 |
| KLEP-EXE-024 | Accepted | After a root Solo Goal's Locks pass against the final immutable post-Tandem snapshot, an optional project-authored evaluator returns exactly one deterministic, finite, signed, inspectable intrinsic-attraction contribution. A Goal without an evaluator contributes zero. Its pre-Observer score is `BaseAttractiveness` plus every Validation and Execution Lock's authored attractiveness plus that intrinsic contribution; owned-child scores never contribute. Evaluation is read-only and cannot mutate lifecycle, Goal progress, eligibility, Keys, Locks, or another subsystem. Existing certainty, equal-score retention, and strict-higher interruption rules apply. Existing Observer polish remains the separate, final, one-use, positive, eligibility-gated contribution. An evaluator fault or invalid result aborts the Tick, is traceable and rethrown, and cannot make behavior eligible. | Owner clarification, 2026-07-19 |
| KLEP-EXE-025 | Accepted | `DeclaredOutputs` is the one and only authored success-effect declaration. Before a run may be accepted as `Succeeded`, its cumulative Add/Replace emissions for that run must contain every declared stable Key ID; an Enter or earlier Running emission counts. Omission is a programming fault and the terminal success is not accepted. An Executable may not Add or Replace an undeclared ID. An empty declaration is valid. No separate `CompletionEffects`, possible-output, or conditional-output list exists in this contract. | Owner clarification that declared output and completion effect are the same fact, 2026-07-19 |
| KLEP-EXE-026 | Accepted | The Agent exclusively owns every Executable runtime, initialization/teardown, Tandem settlement, Solo eligibility and scoring, interruption, advancement, Goal progress, output-publication timing choice, patient state, and immutable decision/fault trace. One Agent Tick is the sole Core decision/fire path. Existing deterministic lifecycle, exact-once cleanup, stable ordering, wave barriers, failure, and interruption behavior is preserved with Agent replacing Neuron as its owner. | Owner architecture correction, 2026-07-19 |
| KLEP-EXE-027 | Accepted | A Goal may be a root Solo or root Tandem Executable. A Solo Goal is scored, may receive intrinsic and eligible Observer influence, and competes in the single Solo lane. A Tandem Goal is an unscored automatic composite: when its Locks pass it advances with the Tandem field at most once per Agent Tick and receives no attraction or Observer-polish component. The root Goal's mode controls its activation, forwarded-child, and completion-output visibility boundary. | Owner octopus/Tandem-Goal clarification, 2026-07-19 |
| KLEP-EXE-028 | Accepted | A Goal stores immutable authored layers and exclusive child definitions, while its owning Agent stores all mutable Goal and descendant runtime state. One root Goal considers at most one layer per Agent Tick; accepted All/Any/None progress, retry, cancellation, nested-fault, and cleanup rules remain unchanged. A child's authored execution mode does not create an independent root scheduling lane while that child is Goal-owned. | Owner Agent/Neuron responsibility correction, 2026-07-19 |
| KLEP-AGENT-001 | Superseded | `KLEPAgent` is a pure Core Tick facade over one Neuron. One Agent Tick advances its Neuron exactly once and then observes the immutable decision trace; there is no Agent `Update`, `FixedUpdate`, Unity component, or event bridge. | Superseded by accepted KLEP-AGENT-008, 2026-07-19 |
| KLEP-AGENT-002 | Accepted | An Agent environment signature is the ordinally sorted unique set of visible `(scope, stable Key ID)` pairs in the Neuron's final post-Tandem snapshot. Tick/wave metadata, payloads, occurrence IDs, sources, lifetimes, and duplicate counts do not participate. | Owner approval, 2026-07-18 |
| KLEP-AGENT-003 | Accepted | The learning table is keyed by environment signature and root Solo stable ID; a Goal is learned as its root Goal ID, never as one of its owned children. A completed transition uses semi-Markov Q-learning with configurable defaults `alpha = 0.2`, `gamma = 0.9`, and elapsed top-level Tick duration `d`: `Q <- Q + alpha * (reward + gamma^d * maxEligibleNextQ - Q)`. | Owner approval, 2026-07-18 |
| KLEP-AGENT-004 | Accepted | The configurable default rewards are `+1` for a Succeeded Solo run, `-1` for a Failed run, and `-0.25` for cancellation specifically caused by `Interrupted`. Other cancellation reasons, removal, and faults supply no learning sample. | Owner approval, 2026-07-18 |
| KLEP-AGENT-005 | Accepted | State familiarity is `N / (N + scale)`, where `N` is the count of prior Agent-Tick observations of that signature and the configurable scale defaults to `4`. Navigation confidence is familiarity multiplied by the eligible state's normalized positive maximum Q-value: `confidence = familiarity * clamp01(max(0, maxEligibleQ) / positiveQBound)`, where `positiveQBound = successReward / (1 - gamma)` and is `10` under the defaults. | Owner approval, 2026-07-18 |
| KLEP-AGENT-006 | Accepted | Confidence is diagnostic only in this slice. Confidence at or below the configured guidance threshold emits an immutable guidance request. The threshold defaults to `0`, so an unseen environment with zero confidence requests guidance. Neither confidence nor that request changes eligibility, authored score, certainty threshold, Solo selection, switching, Keys, or lifecycle. | Owner approval, 2026-07-18 |
| KLEP-AGENT-007 | Superseded | Full Planner execution, Nora/Aron breadth-versus-depth competition, model trust/resource allocation, persistence, and neural implementations remain outside the Agent slice. The later accepted OBS-001 through OBS-006 add only the optional one-use Observer boundary. | Superseded by accepted KLEP-OBS-010 and KLEP-OBS-011, 2026-07-19 |
| KLEP-AGENT-008 | Accepted | Exactly one `KLEPAgent` exclusively claims one passive `KLEPNeuron` and owns its deterministic decision/fire clock. `KLEPAgent.Tick` commits one guarded Local boundary, applies a validated catalog revision, advances runtimes, records the decision, then performs learning and optional deliberation. A Neuron has no independent Tick path; Unity remains a thin host that calls the Agent once. | Owner architecture correction, 2026-07-19 |
| KLEP-AGENT-009 | Accepted | The default designer-authored projected-satisfaction policy evaluates an immutable desired Key/Lock expression for current and Observer-projected states. Each desire traces finite weight, optional finite pressure defaulting to `1`, current truth `0 or 1`, and either projected truth `0 or 1` from a complete successful-run-completion state or explicit unknown from abstention. Complete projections contribute `weight * pressure * (projected - current)`; checked finite contributions are summed over already-eligible Solo candidates. Abstention contributes exactly zero. Unit weights/pressures reproduce net satisfied-desire change for complete projections. Project vocabulary and weights remain designer-owned. | Owner weighted-needs clarification, 2026-07-19; final-state evidence clarification, 2026-07-19 |
| KLEP-OBS-001 | Superseded | The Agent consults an optional pure Observer only after a completed Tick emits a guidance request. The Observer cannot advance the Agent or Neuron. Its advice is prepared for the following top-level Agent Tick and can never alter the decision that requested it. | Superseded by accepted KLEP-OBS-010 and KLEP-OBS-011; low-confidence guidance remains as its optional deliberation branch, 2026-07-19 |
| KLEP-OBS-002 | Accepted | Polishing is an immutable, inspectable score contribution associated with one root Solo Goal or action and its authored Locks. It never mutates Lock attractiveness, definitions, Keys, Q-values, eligibility, or lifecycle. Advice is offered once and tarnishes after that next-Tick use. | Owner approval, 2026-07-18 |
| KLEP-OBS-003 | Accepted | One-use advice applies only when the following Tick's final post-Tandem environment signature exactly matches the environment that requested guidance. A changed environment makes the advice stale. A missing, same-ID replacement, or ineligible target receives no score contribution. | Owner approval plus registration-tenure safety correction, 2026-07-18 |
| KLEP-OBS-004 | Accepted | Applied Observer polish participates in the final score before certainty and Solo arbitration. It may lift an already-eligible target above the certainty threshold or let a strictly higher influenced challenger interrupt the Running Solo; an equal effective score retains the current Solo. | Owner approval, 2026-07-18 |
| KLEP-OBS-005 | Accepted | The first Observer chooses a holistic direction among currently eligible root Solo Goals and actions from deterministic, provenance-bearing project evidence. Memory, emotional well-being, physical needs, and authored urgency are optional evidence sources rather than hardcoded Core meanings. The Observer may abstain when no supplied evidence supports a beneficial direction. | Owner holistic-direction clarification, 2026-07-18 |
| KLEP-OBS-006 | Superseded | Memory remains an independent system and may be queried only through an injected read-only evidence boundary. A separate Planner, Nora/Aron competition, model trust, and multi-step graph search remain deferred; higher reasoning lives behind the Observer boundary. | Superseded by accepted KLEP-OBS-010 and KLEP-OBS-011; Memory independence remains protected, 2026-07-19 |
| KLEP-OBS-007 | Accepted | Observer V1 uses one configurable finite positive polish amount, accepts at most one signed net contribution per source and target, sums evidence deterministically by source ID, chooses the greatest total strictly above a configurable nonnegative floor (default zero) with root stable ID as tie-break, and supplies no advice when there are no eligible candidates or no supported total above the floor. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-OBS-008 | Accepted | The Agent still emits a request on every completed low-confidence Tick, but consults its Observer at most once per unchanged uncertainty episode. A changed environment signature, changed guidance evidence fingerprint, changed eligible-ID set, rejected same-ID target replacement, committed learning update, or confidence recovery followed by a later dip permits another consultation. | Owner acceptance of the tested V1 contract, refined by KLEP-OBS-009, 2026-07-19 |
| KLEP-OBS-009 | Accepted | Guidance carries a deterministic evidence fingerprint separate from the Agent's presence-only learning environment. It includes visible Key scope, stable ID, ordered payload field names/types/values, and duplicate payload occurrences while excluding occurrence authority, provenance, lifetime, and Tick/wave metadata. A changed fingerprint rejects prepared advice as `StaleEvidence` and reopens consultation; semantically identical payload reissue does neither. Lock eligibility and Q-table identity remain unchanged. | Owner-requested payload-sensitive guidance plus focused Agent/Observer regressions, 2026-07-19 |
| KLEP-OBS-010 | Accepted | The Observer owns pure validation and mapping of the Neuron's complete recursive Executable graph. Every Agent has this structural service; a deterministic baseline supplies it when no higher-cognition implementation is injected. It receives immutable roots, Goal descendants, modes, Locks, guaranteed DeclaredOutputs, registration tenures, and catalog revision; it rebuilds on revision change or explicit request and may otherwise reuse an exact assessment. It returns validity, inspectable reachability/map evidence, and optional candidate projections without mutating, opening Locks, selecting, or firing. DeclaredOutputs are cumulative successful-run emissions, not proof of final persistence or coexistence. A satisfaction projection supplies a complete presence state at the successful-run-completion horizon bound to exact accepted revision/fingerprint, root tenure, target, and current evidence, or explicitly abstains with zero influence and unknown reason. The Agent rejects invalid revisions and stale/mismatched projections and alone decides. | Owner Planner-in-Observer and executable-map clarification, 2026-07-19; final-state evidence clarification, 2026-07-19 |
| KLEP-OBS-011 | Accepted | Catalog validation/mapping is independent of confidence and occurs whenever required even in a familiar high-confidence state. Low confidence remains diagnostic and may request optional additional Observer deliberation; it is not the trigger for keeping the structural map truthful and does not itself alter selection. Nora/Aron breadth-versus-depth competition is not required because the graph is explicitly mapped; it remains an optional future search strategy behind the Observer boundary. | Owner low-confidence and Nora/Aron clarification, 2026-07-19 |
| KLEP-ETHICS-001 | Accepted | KLEP does not supply one correct ethical system. A higher-cognition designer supplies the contextual evaluation that gives an observed or self-authored action meaning; the same physical action may therefore produce different evaluations in different contexts. | Owner clarification, 2026-07-18 |
| KLEP-ETHICS-002 | Accepted | KLEP supplies a portable Ethics springboard rather than project morality: a project may use simple weighted contextual rules, implement richer rules, replace the evaluator, or roll its own policy while retaining an inspectable handoff to Emotion. Humans-versus-Zombies judgments remain future project policy. | Owner request, 2026-07-18 |
| KLEP-ETHICS-003 | Accepted | One Ethics request carries transient project-owned immutable context input, an immutable context ID/schema identity, evaluation ID and Tick, cause origin, and target Emotion configuration. The completed result retains identity and trace rather than the arbitrary live context. The guarded wrapper owns final influence provenance; evaluator code cannot advance Emotion or behavior selection. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-ETHICS-004 | Accepted | The reference weighted evaluator requires exact authored Emotion axis names, traces its bias and every rule once in supplied order, computes applied contribution as normalized impulse times a finite `[0, 1]` weight, retains raw double-precision totals, and clamps only the final Emotion impulse. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-ETHICS-005 | Accepted | Every evaluator declares stable ID, version, and expected Emotion axes. The guarded boundary checks those declarations before and after project policy code, rejects axis mismatch before callback execution, and copies stable evidence references into each trace. Arbitrary project evaluator and rule callbacks remain subject to an explicit purity obligation. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-EMOTION-001 | Accepted | Emotion integrates already-evaluated internal or external influences on a two-axis graph whose axis names and meanings belong to the higher-cognition designer. Emotion does not perform the ethical evaluation itself. | Owner clarification, 2026-07-18 |
| KLEP-EMOTION-002 | Accepted | Emotion has inspectable position and velocity. Influence changes velocity, velocity changes position, and friction must bring unsupported motion to exact rest without silently returning position to neutral. Immutable snapshots expose change and how long position has remained unchanged. | Owner clarification, 2026-07-18 |
| KLEP-EMOTION-003 | Accepted | The first isolated Emotion runtime uses normalized `[-1, 1]` axes, bounded speed, linear friction per supplied consecutive Tick, and a bounded recent snapshot window. It does not alter Keys, Locks, eligibility, scores, Executables, Goals, Agent learning, or lifecycle. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-MEMORY-001 | Accepted | The subsystem is named `KLEPMemory`. It does not use `SLASHBridge`; loose-coupling infrastructure may be reconsidered later without being invented inside Memory now. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-002 | Accepted | One remembered experience contains the Neuron's perceived Key state before an event, zero or more ordered intermediate states, and the perceived consequence state. Those Keys are the Agent's inner representation of both external and internal stimuli. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-003 | Accepted | A new detailed memory preserves whole perceived context, including duplicate Key occurrences and payload evidence. With age, payload and occurrence detail may wash away while stable Key identities and accumulated pattern facts remain. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-004 | Accepted | Only an actual terminal `Succeeded` Executable result is action success. Ethics evidence and produced Emotion state are separate remembered facts; neither can manufacture, erase, or rewrite lifecycle success. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-005 | Accepted | Memory heat represents both freshness and repetition. Repeated or recent patterns remain in working memory; cold patterns are either forgotten or consolidated into deep storage according to explicit retention evidence. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-006 | Accepted | Large positive and negative Emotion swings are both emotionally salient. One traumatic episode may be retained without dominating every recall; repeated trauma of the same pattern becomes durable for the lifetime of the persisted Memory. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-007 | Superseded | Only Sensors produce Keys. Memory records and returns recall evidence; it does not emit a Key, mutate a KeyStore, open a Lock, or directly make an Executable eligible. A future recall Sensor may translate an explicit recall result at a normal Sensor boundary. | Narrowed by accepted KLEP-MEMORY-016 to resolve conflict with accepted Executable outputs, 2026-07-19 |
| KLEP-MEMORY-008 | Accepted | Memory retains the produced Emotion state of an experience. The higher-cognition evidence layer may compare remembered results with an Agent-specific preferred emotional position and stability tolerance; KLEP does not assume that neutral `(0,0)` is universally desirable. | Owner clarification, later connected by KLEP-COG-002, 2026-07-19 |
| KLEP-MEMORY-009 | Accepted | Similar repeated experiences retain the newest concrete exemplars and the most emotionally intense concrete exemplars while older ordinary episodes may survive only in aggregate gist. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-010 | Accepted | Memory belongs to one Agent. Removing a dead Agent may discard its Memory; logging out may export and later restore the Memory subsystem at the same explicit KLEP Tick. Offline wall time does not silently age Memory. | Owner clarification, 2026-07-18 |
| KLEP-MEMORY-011 | Accepted | The first pure Memory layer is a one-way sibling of Core, Ethics, and Emotion. An experience's projector signature retains `Prior`, `During`, and `Consequence` roles; association compares the complete phase-aware pattern with an explicit reversal guard, while current-context recall compares only prior-state cells. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-MEMORY-012 | Accepted | Similar experiences with the same causal action identity reinforce one deterministic projector cluster using configurable Jaccard overlap. Each cluster retains phase-aware Key hit counts, lifecycle outcome counts, exact emotional accumulator state, recent exemplars, memorable exemplars, and an inspectable transition trace. Experiences supplied together are applied by Consequence Tick and wave, with ordinal ID only as an exact-time tie-break. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-MEMORY-013 | Accepted | Memory heat adds configured freshness and repetition increments, cools by caller-supplied Tick distance, and never includes emotional salience. Emotional swing is separately traced and may consolidate a memory immediately; repeated trauma may make a cluster indelible under automatic archive pressure. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-MEMORY-014 | Accepted | Associative recall is a pure, non-authoritative query over prior-state overlap, repetition, freshness, emotional salience, and optional destination-plus-velocity preference affinity. Preference uses root-mean-square distance across actual produced positions and mean non-cancelling speed so opposite outcomes cannot manufacture a fictional preferred state at rest. Recall never strengthens Memory or changes Core; the later accepted KLEP-COG-002 supplies its first eligibility-gated evidence seam. | Owner acceptance of the tested V1 contract, refined by KLEP-COG-002, 2026-07-19 |
| KLEP-MEMORY-015 | Accepted | `KLEPMemoryState` is a versioned, owner-bound immutable continuation state for the Memory subsystem. It preserves clusters, exact accumulators, exemplars, seen experience IDs, Tick, policy, next deterministic sequence, last transition trace, and bounded snapshot history. Public validated constructors provide a project-owned rehydration boundary; a built-in file codec and whole-Agent save composition remain outside the pure layer. | Owner acceptance of the tested V1 contract, 2026-07-19 |
| KLEP-MEMORY-016 | Accepted | Memory itself never emits or stages Keys. External world observations enter through Sensors; explicit initialization and accepted Executable output boundaries may also create Key occurrences. Recall remains non-authoritative, cannot open a Lock or make behavior eligible, and may become internal stimulus only through a future explicit recall Sensor at a normal Key boundary. | Owner-directed authority reconciliation preserving the Memory boundary, 2026-07-19 |
| KLEP-MEMORY-017 | Accepted | A configured positive Memory cooling amount must make representable progress. If positive elapsed cooling rounds away at Single precision, heat snaps to exact zero and the transition records the actual removed heat; ordinary same-Tick forgetting or archive policy then resolves that cold cluster. | Owner-requested precision correction plus focused regression, 2026-07-19 |
| KLEP-COG-001 | Accepted | The first portable cognition coordinator consumes explicit observed causal moments and project context, then performs one traced Ethics evaluation, one exactly consecutive Emotion advance from that evaluation, and one strictly later Memory record containing the factual lifecycle outcome, Ethics trace, and produced Emotion consequence. It does not Tick a Neuron or Agent, select behavior, or share hidden subsystem clocks. | Owner-specified Ethics to Emotion to Memory direction plus focused cognition regression, 2026-07-19 |
| KLEP-COG-002 | Accepted | Memory and Emotion reach Observer only through read-only, policy-owned evidence adapters evaluated for the Observer request's already-eligible root targets. Memory policy supplies and interprets recall cues; Emotion policy interprets a copied state. Both retain identity, version, explanation, and evidence provenance, may abstain, and cannot mutate their source, emit Keys, open Locks, or assign universal emotional or ethical meaning. | Owner-requested higher-cognition arrangement plus focused cognition regression, 2026-07-19 |
| KLEP-COG-003 | Accepted | `KLEPCognitionComposition<TContext>` is the engine-free production composition root that consistently wires and retains Ethics, Emotion, Memory, their coordinator, the two standard evidence adapters, and one Observer. This is not exclusive capability ownership: the trusted host retains the injected objects, must treat `Process` as their causal write boundary, and must not share Agent-specific mutable cognition state across Agents. A host injects the Observer into an Agent; the composition receives no Neuron execution authority. | Owner-requested portable springboard plus focused cognition regression, 2026-07-19 |
| KLEP-COG-004 | Accepted | After causal preflight and before Ethics evaluation, `Process` captures owner-bound opaque checkpoints of the exact Emotion and Memory instances. If any later evaluation, advancement, recording, or transition-construction step throws, both subsystem clocks, mutable state, current snapshots, and bounded histories are restored in place; no new `LastTransition` is published, and the same causal request may be retried. This is internal composition authority, not public rewind or persistence, and it cannot roll back project-owned evaluator side effects. | Owner-requested gap closure plus hostile fault-and-retry cognition regression, 2026-07-19 |
| KLEP-BEH-001 | Superseded | A world-observation behavior has two parts: a Unity adapter samples external state before the decision boundary, and a pure Core Tandem Executable publishes only the supplied observation during `KLEPNeuron.Tick`. Physics and Unity object access never occur inside Core. | Superseded in Tick ownership by KLEP-BEH-011 and accepted KLEP-AGENT-008, 2026-07-19 |
| KLEP-BEH-002 | Superseded | The Ground Sensor publishes one Local `OneCycle` Ground occurrence when its sampled overlap is true and publishes nothing when false. The Tandem wave makes a true sample visible to Solo selection in the same Tick; the prior occurrence expires before a later false Tick. | Superseded in successful-completion semantics by accepted KLEP-EXE-025; observation intent must be re-authored without a successful omitted declaration, 2026-07-19 |
| KLEP-BEH-003 | Accepted | The first zombie test must observe an enemy, use Ground in its Key/Lock chain, move toward the enemy, and attack it. | Owner request, 2026-07-18 |
| KLEP-BEH-004 | Superseded | One Enemy Sensor sample emits zero or more Local `OneCycle` `EnemyDetected` occurrences with immutable `entityId`, `teamId`, distance, and XYZ payload fields. Collider duplicates collapse by entity, same-team/self/dead entities are excluded, and samples are ordered by ordinal entity ID. | Superseded as a Candidate by accepted KLEP-EXE-025; an empty successful sample cannot omit its declaration, 2026-07-19 |
| KLEP-BEH-005 | Superseded | A Tandem target Router requires Ground and EnemyDetected, chooses minimum sampled distance with ordinal entity ID as the tie-break, and emits exactly one mutually exclusive Local `OneCycle` `MoveTarget` or `AttackTarget`. Ground plus MoveTarget enables a Running Move Solo; Ground plus AttackTarget enables an instantaneous Attack Solo. | Superseded by accepted KLEP-EXE-025; mutually exclusive successful branches require separately truthful Executables or a later explicit contract, 2026-07-19 |
| KLEP-BEH-006 | Accepted | The zombie-test target is controlled through KLEP: keyboard Sensors observe W/A/S/D movement and a mouse Sensor observes the world aim point; a KLEP behavior moves the target forward/back/left/right and rotates it toward that mouse observation. | Owner request, 2026-07-18 |
| KLEP-BEH-007 | Accepted | Keyboard hold observations are separate Local `OneCycle` W/A/S/D presence Keys and mouse aim is one Local `OneCycle` immutable XYZ payload. One continuous Locomotion Solo requires Ground plus MouseAim, reads movement Keys optionally, cancels opposing directions, normalizes diagonals, and produces one cycle-bound rotate-and-move intent so aiming and movement never compete as separate Solos. | Owner acceptance of the tested pure behavior contract, 2026-07-19 |
| KLEP-BEH-008 | Accepted | The zombie showcase has three scored root Goals: Avoid Edge `100`, Eat Human `50`, and Wander `10`. Eat Human owns one `AnyCanFire` layer ordered Attack then Move; the other Goals each own one continuous action child. Root Goal identity explains why an action is pursued and the owned child explains how. Eat Human and Wander remain concurrently eligible when a human is observed so the strictly higher Eat Human score visibly interrupts Wander. EdgeDanger closes both and positively opens Avoid Edge, making safety a Lock fact rather than a score-only preference. | Owner showcase request, 2026-07-19 |
| KLEP-BEH-009 | Accepted | Edge evidence is sampled by eight individually issued Unity downward raycasts in fixed clockwise order and reduced outside Unity into one immutable Local `OneCycle` EdgeDanger payload only when support is incomplete. The payload copies supported/missing bitmasks, missing count, and a finite normalized local avoidance vector; symmetric missing patterns use a deterministic ordinal fallback. | Owner edge-raycast request translated at the accepted observation boundary, 2026-07-19 |
| KLEP-BEH-010 | Accepted | With Ground present and EdgeDanger absent, a pure deterministic Router emits one Local `OneCycle` WanderDirection from a fixed eight-heading table. Wander therefore remains an eligible baseline while Eat Human can outrank it. An explicit seed and positive segment length determine repetition; no engine randomness, string hash, or wall time participates. | Owner Wander Goal request plus deterministic Core requirement, 2026-07-19 |
| KLEP-BEH-011 | Candidate | A world-observation behavior has two parts: a Unity adapter samples external state before the decision boundary, and a pure Core Tandem Executable publishes only the supplied observation during its owning `KLEPAgent.Tick`. Physics and Unity object access never occur inside Core. | Agent-owned replacement for superseded KLEP-BEH-001 |
| KLEP-UNITY-001 | Candidate | `KLEPNeuronRunner` is an optional Unity frame host, not a second Core clock. It samples owned observation sources in ordinal stable-ID order and then calls `KLEPAgent.Tick` exactly once. Sensors have no `Update` or `FixedUpdate`; an external host may disable automatic ticking and call `TickOnce`. | Unity integration boundary for the Ground Sensor slice |
| KLEP-UNITY-002 | Candidate | A runner may build pure Executables from stable-ID-ordered Unity providers before its first Tick and invoke every registered stable-ID-ordered Unity effect sink after one completed Agent Tick. Registered providers/sinks must remain enabled on an active hierarchy. A sink observes the immutable trace and cannot change the decision that produced it; a post-Tick sink fault latches further ticking so its queued Core outcome cannot be learned. Zombie movement is configured in units per Tick; attack damage is applied once only when that Tick contains the Succeeded Attack Solo step. | Zombie Unity adapter boundary |
| KLEP-UNITY-003 | Superseded | Unity's Input System is sampled only through runner-owned observation sources before the target Agent Tick. Keyboard and mouse components have no independent `Update`/`FixedUpdate`, and only the post-Tick locomotion sink may mutate the controlled Transform. Movement is authored in units per Neuron Tick rather than seconds. Hardware-specific Input System sources remain in the demo assembly, so the production KLEP package gains no Input System dependency. | Superseded in unit wording by KLEP-UNITY-007 and accepted KLEP-AGENT-008, 2026-07-19 |
| KLEP-UNITY-004 | Candidate | The two-Neuron zombie demo uses an explicit world host with both runners' automatic Tick disabled. Each frame it synchronizes prior transforms, advances the target input Neuron, synchronizes the target effect, then advances the zombie Neuron. Cross-Neuron observation therefore has declared order instead of depending on Unity component `Update` order or physics auto-sync. | Deterministic target-plus-zombie integration |
| KLEP-UNITY-005 | Superseded | Before registration, `KLEPNeuronRunner` may receive one programmatic configuration/Observer pair or discover exactly one active owned `IKLEPAgentCompositionSource`. It rejects repeated programmatic assignment and conflicting, multiple, inactive, or null sources, then constructs the Agent with the supplied dependencies. A source must be usable from the runner's `Awake` without depending on its own `Awake`; the first successful result is cached across later initialization failure, while a source call that itself fails may be retried and must be retry-safe. The source receives no Neuron handle, absence preserves default/no-Observer behavior, and trusted hosts must not share mutable Agent-specific cognition state across runners. | Superseded by KLEP-UNITY-008 and accepted KLEP-OBS-010, 2026-07-19 |
| KLEP-UNITY-006 | Candidate | A narrow Unity controller may retain its pure Goal-owned action objects and apply only an intent whose recorded cycle equals the immutable completed Agent Tick. Goal children remain absent from Neuron root registration and visible recursively in diagnostics. This project adapter does not create generic Unity Goal authoring or grant the sink selection authority. | Zombie Goal showcase integration |
| KLEP-UNITY-007 | Candidate | Unity's Input System is sampled only through runner-owned observation sources before the target Agent Tick. Keyboard and mouse components have no independent `Update`/`FixedUpdate`, and only the post-Tick locomotion sink may mutate the controlled Transform. Movement is authored in units per Agent Tick rather than seconds. Hardware-specific Input System sources remain in the demo assembly, so the production KLEP package gains no Input System dependency. | Agent-owned replacement for superseded KLEP-UNITY-003 |
| KLEP-UNITY-008 | Candidate | Before registration, `KLEPNeuronRunner` may receive one programmatic Agent configuration and higher-cognition Observer/composition or discover exactly one active owned composition source. Absence preserves the mandatory deterministic baseline structural Observer, not absence of graph validation; optional low-confidence deliberation and project cognition may remain absent. Existing conflict, caching, retry-safety, no-Neuron-handle, and no-shared-Agent-state rules remain. | Structural-Observer replacement for superseded KLEP-UNITY-005 |
| KLEP-DIAG-001 | Candidate | Every completed or faulted Core decision trace may retain a stable-ID-ordered immutable projection of every committed root Executable runtime and recursively owned Goal child. The projection copies state, run/result, Goal progress, and string-only fault evidence; it exposes no runtime controller or lifecycle callback. | Universal Observatory implementation |
| KLEP-DIAG-002 | Candidate | `KLEPNeuronRunner` retains a bounded recent history of completed and faulted Agent traces and separately records whether each trace's Unity sink phase succeeded, faulted, or was not reached. Diagnostics poll this history and are not registered as effect-critical Tick sinks. | Universal Observatory implementation |
| KLEP-DIAG-003 | Candidate | Emotion, Ethics, Memory, Observer, Key exchanges, Key requests, recalls, and project evidence enter the Unity Observatory only through an outward-only explicit probe. Attachment and publication do not Tick, evaluate, recall, exchange, select, score, or mutate the attached systems. | Universal Observatory implementation |
| KLEP-DIAG-004 | Candidate | Core cycle, Tandem wave, Observer request cycle, Emotion Tick, Ethics evaluation Tick, Memory Tick, and project clocks remain distinct. Observatory capture sequence is ordering evidence only and never asserts global simultaneity. | Universal Observatory implementation |
| KLEP-DIAG-005 | Candidate | The Editor Observatory is a read-only evidence viewer. It may filter, freeze, select retained frames, format, and copy reports; it must never refresh itself by advancing a system or reevaluating historical Locks, scores, Ethics, Observer, Memory recall, or Key operations. | Universal Observatory implementation |

## Still open

- Re-authoring conditional Sensors and Routers whose historical successful runs
  emitted none or only one of several declared outputs. Each successful
  Executable must now keep one truthful `DeclaredOutputs` promise; a false sample
  may remain non-successful, or conditional branches may become separately
  locked Executables, but that behavior-library choice is not yet accepted.
- Whether emitted occurrences may always override a definition's default lifetime.
- Reservation, use counts, borrowing and return beyond the explicit V1
  operations, and winning-candidate consumption.
- Whether untrusted agent code needs a capability layer that restricts direct
  mutation API and Neuron-handle access. V1 assumes trusted host orchestration.
- The generic stable entity-handle and Unity registry contract for legacy
  Vector/Transform/object payload uses. The zombie test uses scene-authored text
  IDs and the Enemy Sensor's explicitly referenced latest-sample map only; that
  test-scoped resolver does not settle spawned identity or general registry
  ownership.
- Whether a formal world/tick coordinator should wrap the currently explicit
  Global `KLEPKeyStore.CommitBoundary` call.
- Future score components for Executables, including whether Key attractiveness
  returns and how composite `Any` and `Not` expressions contribute without
  rewarding unrelated Keys.
- Whether Agent confidence itself may later influence a Solo switching margin.
  Observer polish is now a separate accepted score component and does not use
  confidence as its magnitude.
- The project context and policy rules for the Humans-versus-Zombies Ethics
  experiment, including the observed evidence and causal timestamps those rules
  require.
- Additional proposal sources beyond the accepted Observer map/projection and
  KLEP-COG-002 Memory/Emotion evidence, without letting any proposal bypass
  Locks.
- The project-owned file codec and atomic save boundary for `KLEPMemoryState`,
  plus whole-Agent composition with live Keys, Executable lifecycle, Emotion,
  and Agent learning.
- Open-experience persistence when logout occurs after a Prior or During moment
  but before a factual consequence has been observed.
- Richer projection semantics for payload values, conditional external
  observation, cost, and genuinely invented definitions. Complete presence
  projections can represent removal, but the accepted Key-ID
  graph does not require Nora/Aron competition; optional search/resource
  strategies remain deferred behind the Observer boundary.
- Whether an unchanged low-confidence episode may also reconsult on an explicit
  external evidence revision or deterministic cooldown. Payload changes in
  visible Keys are now covered by KLEP-OBS-009; a Memory, Emotion, or project
  evidence change not represented in those Keys still has no revision seam.
- Whether a holistic direction remains a fixed-strength nudge that may lose
  ordinary arbitration, or should receive enough bounded influence to guarantee
  selection among currently eligible targets.
- Global same-world-tick influence across several Neurons; this requires an
  explicit world coordinator rather than a Neuron-owned tandem wave.
